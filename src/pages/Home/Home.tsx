import { Typography } from '@mui/material';

const Home = () => {
  return (
    <div>
      <Typography variant="h1" sx={{ color: 'rgb(34, 197, 94)' }}>
        {/* Main Content */}
      </Typography>
    </div>
  );
};

export default Home;
